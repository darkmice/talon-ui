---
title: Tooltip
description: Dark-background hint label that appears on hover or focus.
sidebar:
  order: 35
---

import ComponentPreview from '@/components/ComponentPreview.astro';
import Basic from '@/examples/tooltip/basic.tsx';
import Sides from '@/examples/tooltip/sides.tsx';
import OnIcon from '@/examples/tooltip/on-icon.tsx';

`Tooltip` is the dark, low-contrast hint that appears next to its trigger. Wraps `@radix-ui/react-tooltip`.

## Basic

<ComponentPreview src="examples/tooltip/basic.tsx" title="Hover for tooltip" height="120px">
  <Basic client:load />
</ComponentPreview>

## Sides

<ComponentPreview src="examples/tooltip/sides.tsx" title="Four placement sides" height="160px">
  <Sides client:load />
</ComponentPreview>

## On an icon trigger

<ComponentPreview src="examples/tooltip/on-icon.tsx" title="Inline help" height="100px">
  <OnIcon client:load />
</ComponentPreview>

## Usage

`Tooltip` requires a parent `<TooltipProvider>`. Place it once at your app root — usually wrapping the entire component tree:

```tsx
<TooltipProvider delayDuration={200} skipDelayDuration={300}>
  <App />
</TooltipProvider>
```

`delayDuration` controls how long users hover before the tooltip appears; `skipDelayDuration` controls how long after closing one tooltip the next one opens instantly.

## Props (highlights)

`TooltipContent` accepts all Radix Tooltip.Content props including `side` (`'top' | 'right' | 'bottom' | 'left'`), `align`, `sideOffset`, `alignOffset`, `arrowPadding`, `sticky`, `hideWhenDetached`, etc.

## Don't

- Don't use Tooltip for critical information — it's hidden by default and not reachable on touch devices.
- Don't ship without `<TooltipProvider>` — the tooltip will silently fail to render.
