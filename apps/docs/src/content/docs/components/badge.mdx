---
title: Badge
description: Small numeric counter or dot positioned on a container's corner.
sidebar:
  order: 7
---

import ComponentPreview from '@/components/ComponentPreview.astro';
import PropsTable from '@/components/PropsTable.astro';
import Number from '@/examples/badge/number.tsx';
import Dot from '@/examples/badge/dot.tsx';
import Overflow from '@/examples/badge/overflow.tsx';

`Badge` is a 12-tall counter pill or 8×8 dot. It works standalone or wraps another element to sit at its top-right.

## Number

<ComponentPreview src="examples/badge/number.tsx" title="Tones" height="100px">
  <Number client:load />
</ComponentPreview>

## Dot

<ComponentPreview src="examples/badge/dot.tsx" title="Status dot on container" height="100px">
  <Dot client:load />
</ComponentPreview>

## Overflow

<ComponentPreview src="examples/badge/overflow.tsx" title="Max overflow" height="120px">
  <Overflow client:load />
</ComponentPreview>

When `count` exceeds `max` (default 99), the badge renders `max+`. Set `max` to control the cutoff.

## Props

<PropsTable component="Badge" />

## Don't

- Don't combine `dot` with a large `count` value — pick one shape.
- Don't use `Badge` for status pills longer than 4 digits; switch to `Tag`.
