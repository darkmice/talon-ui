---
title: Textarea 多行文本
nav: 组件
group: 基础组件
order: 3
---

# Textarea 多行文本

带可选自动调高功能的多行文本输入框。`Textarea` 与 `Input` 共享相同的色调/尺寸/禁用规则。设置 `autosize` 可使高度随内容自动增长。

## 基础

<code src="./demos/basic.tsx"></code>

## 自动调高

<code src="./demos/autosize.tsx"></code>

## API

<API id="Textarea"></API>

所有原生 `<textarea>` 属性均可使用（`size` 除外）。
